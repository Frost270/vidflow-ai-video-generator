def expand_prompt(prompt):

    scenes = [
        f"{prompt}, wide cinematic shot",
        f"{prompt}, closer view",
        f"{prompt}, dramatic lighting",
        f"{prompt}, action scene"
    ]

    return scenes